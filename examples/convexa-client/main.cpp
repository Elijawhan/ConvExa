#include <convexa.h>
#include <MathOperation.h>

#include <iostream>

int main() {
    hello_amazing_convexa_world();

    MathOperation math_operation(0);
    math_operation(nullptr, nullptr);

    return 0;
}
